---
import {
  Search,
  ChevronRight,
  ChevronLeft,
  ChevronUp,
  ChevronDown,
  Sun,
  Moon,
  File,
  Terminal,
  House,
  Calendar,
  Clock,
  Folder,
  X,
  Menu,
  Plus,
  ChartColumnStacked,
} from '@lucide/astro';

export interface Props {
  name:
    | 'search'
    | 'chevron-right'
    | 'chevron-left'
    | 'chevron-up'
    | 'chevron-down'
    | 'sun'
    | 'moon'
    | 'file'
    | 'command'
    | 'home'
    | 'calendar'
    | 'clock'
    | 'folder'
    | 'chart-column-stacked'
    | 'x'
    | 'menu'
    | 'plus';
  class?: string;
  size?: number;
}

const { name, class: className = '', size = 16 } = Astro.props as Props;

const iconMap: Record<string, any> = {
  search: Search,
  'chevron-right': ChevronRight,
  'chevron-left': ChevronLeft,
  'chevron-up': ChevronUp,
  'chevron-down': ChevronDown,
  sun: Sun,
  moon: Moon,
  file: File,
  command: Terminal,
  home: House,
  calendar: Calendar,
  clock: Clock,
  folder: Folder,
  'chart-column-stacked': ChartColumnStacked,
  x: X,
  menu: Menu,
  plus: Plus,
};

const LucideIcon = iconMap[name];
if (!LucideIcon) return;
---
<LucideIcon class={className} size={size} />
